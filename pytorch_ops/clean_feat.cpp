#include <torch/torch.h>

// """
// Args:
// base_feat: shape(N,C,H,W)
// rois: shape(N,M,5)
// Returns:
// clean_feat: shape(N,C,H,W)
// """

at::Tensor clean_feat(at::Tensor rois){
    auto batch_size = rois.size(0);
    auto
    at::Tensor mask()
    for(int i=0;i<num;i++){

    }
    return {};
}
